1.Build docker image  -       docker build -t mlops-docker-demo .             #mlops-docker-demo is the name of file also add"."
2. Run the container  -       docker run -p 5000:5000 mlops-docker-demo    #5000 is the port name that is mentioned in the docker 
                                                                            code, the 2nd 5000 is the port that it will run on.

3. Give it a tage before we push it into a registry ..docker hub 
    docker tag mlops-docker-demo jubin1925/mlops-docker-demo:v1     


4. to push the image to registry (docker hub)   -   docker push jubin1925/mlops-docker-demo:v1   
5. to pull the image to somewhere eles          -   docker pull jubin1925/mlops-docker-demo:v1   